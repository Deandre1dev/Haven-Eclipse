"""
This module is used to configure the band application.
"""
from django.apps import AppConfig


class BandConfig(AppConfig):
    """
    This module is used to configure the band app.s
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'band'
